var React = require('react');

// TODO: Add React.createClass() style
var Timer = (props) => {
    return (
        <div>
            Timer.jsx Rendered
        </div>
    )
};

module.exports = Timer;